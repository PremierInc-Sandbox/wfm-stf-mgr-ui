import { OffGridActivities } from './off-grid-activities';

describe('OffGridActivities', () => {
  it('should create an instance', () => {
    expect(new OffGridActivities()).toBeTruthy();
  });
});
